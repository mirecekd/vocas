version: '3.8'

services:
  vocas:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./backend:/app
      - ./backend/static/audio:/app/static/audio
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    restart: unless-stopped
